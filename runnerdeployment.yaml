# runnerdeployment.yaml
apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: opendlp-runnerdeploy
spec:
  replicas: 1
  template:
    spec:
      repository: moophlo/opendlp
